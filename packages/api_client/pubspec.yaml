name: api_client
description: A repository to handle all the API calls for the application.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.4

dev_dependencies:
  build_runner: ^2.4.13 
  mocktail: ^1.0.4
  test: ^1.25.7
  very_good_analysis: ^7.0.0

dependencies:
  cloud_firestore: ^5.6.4
  equatable: ^2.0.7
  http: ^1.3.0
  json_annotation: ^4.9.0
  json_serializable: ^6.9.0
