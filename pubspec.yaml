name: white_label
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4
  flutter: 3.24.5

dependencies:
  flutter:
    sdk: flutter
  courses_repository:
    path: packages/courses_repository
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true